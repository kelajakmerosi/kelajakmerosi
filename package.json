{
  "name": "kelajakmerosi",
  "version": "1.0.0",
  "description": "Kelajak Merosi — full-stack edu platform (React + Node.js/Express + MongoDB)",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "concurrently -n CLIENT,SERVER -c cyan,green \"npm run dev:client\" \"npm run dev:server\"",
    "build:client": "cd client && npm run build",
    "typecheck": "cd client && npm run typecheck",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
